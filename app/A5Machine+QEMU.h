//
//  A5Machine+QEMU.h
//  Сборка командной строки QEMU из конфигурации машины.
//
//  Выделено в отдельную категорию, потому что к этому коду особые требования:
//  qemu_init() при неприемлемых аргументах зовёт exit() и уносит весь процесс
//  приложения, а не только поток ВМ.  Значит здесь нельзя просто передавать
//  дальше то, что лежит в конфиге — всё должно быть приведено к заведомо
//  допустимому виду.
//

#import "A5Machine.h"

@interface A5Machine (QEMU)

/// argv без argv[0].  biosPath — каталог с pc-bios внутри бандла.
/// Возвращает nil и заполняет error, если запускать нечего (нет ни одного
/// носителя и не создан жёсткий диск).
- (NSArray *)qemuArgumentsWithBIOSPath:(NSString *)biosPath error:(NSError **)error;

@end
